# joineRML ![joineRML hex logo](reference/figures/hex.png)

`joineRML` is an extension of the joineR package for fitting joint
models of time-to-event data and multivariate longitudinal data. The
model fitted in joineRML is an extension of the Wulfsohn and Tsiatis
(1997) and Henderson et al. (2000) models, which is comprised of
$`(K+1)`$-sub-models: a Cox proportional hazards regression model (Cox,
1972) and a $`K`$-variate linear mixed-effects model - a direct
extension of the Laird and Ware (1982) regression model. The model is
fitted using a Monte Carlo Expectation-Maximization (MCEM) algorithm,
which closely follows the methodology presented by Lin et al. (2002).

## Links

- Package website (pkgdown):
  <https://graemeleehickey.github.io/joineRML/>
- CRAN release: <https://CRAN.R-project.org/package=joineRML>
- Source code (GitHub): <https://github.com/graemeleehickey/joineRML>

## Why use joineRML?

As noted in Hickey et al. (2016), there is a lack of statistical
software available for fitting joint models to multivariate longitudinal
data. This is contrary to a growing methodology in the statistical
literature. `joineRML` is intended to fill this void.

# Example

The main workhorse function is `mjoint`. As a simple example, we use the
`heart.valve` dataset from the package and fit a bivariate joint model.

``` r

library(joineRML)
data(heart.valve)
hvd <- heart.valve[!is.na(heart.valve$log.grad) & !is.na(heart.valve$log.lvmi), ]

set.seed(12345)
fit <- mjoint(
    formLongFixed = list("grad" = log.grad ~ time + sex + hs,
                         "lvmi" = log.lvmi ~ time + sex),
    formLongRandom = list("grad" = ~ 1 | num,
                          "lvmi" = ~ time | num),
    formSurv = Surv(fuyrs, status) ~ age,
    data = list(hvd, hvd),
    timeVar = "time")
```

The fitted model is assigned to `fit`. We can apply a number of
functions to this object, e.g. `coef`, `logLik`, `plot`, `print`,
`ranef`, `fixef`, `summary`, `AIC`, `getVarCov`, `vcov`, `confint`,
`sigma`, `update`, `formula`, `resid`, and `fitted`. In addition,
several special functions have been added, including `dynSurv`,
`dynLong`, and `baseHaz`, as well as plotting functions for objects
inheriting from the `dynSurv`, `dynLong`, `ranef`, and `mjoint`
functions. For example,

``` r

summary(fit)
plot(fit, param = 'gamma')
```

`mjoint` automatically estimates approximate standard errors using the
empirical information matrix (Lin et al., 2002), but the `bootSE`
function can be used as an alternative.

# Errors and updates

If you spot any errors or wish to see a new feature added, please file
an issue at <https://github.com/graemeleehickey/joineRML/issues> or
email [Graeme Hickey](mailto:graeme.hickey@liverpool.ac.uk).

# Further learning

For an overview of the model estimation being performed, please see the
technical vignette, which can be accessed by

``` r

vignette('technical', package = 'joineRML')
```

For a demonstration of the package, please see the introductory
vignette, which can be accessed by

``` r

vignette('joineRML', package = 'joineRML')
```

# Funding

This project is funded by the [Medical Research
Council](https://mrc.ukri.org) (Grant number MR/M013227/1).

![Medical Research Council
logo](https://www.ukri.org/wp-content/themes/ukri/assets/img/ukri-mrc-standard-logo.png)

Medical Research Council logo

# Using the latest developmental version

To install the latest **developmental version**, you will need R version
(version 3.3.0 or higher) and some additional software depending on what
platform you are using.

## Windows

If not already installed, you will need to install
[Rtools](https://cran.r-project.org/bin/windows/Rtools/). Choose the
version that corresponds to the version of R that you are using.

## Mac OSX

If not already installed, you will need to install Xcode Command Line
Tools. To do this, open a new terminal and run

``` bash
$ xcode-select --install
```

## From R

The latest developmental version will not yet be available on CRAN.
Therefore, to install it, you will need `devtools`. You can check you
are using the correct version by running

Once the prerequisite software is installed, you can install `joineRML`
by running the following command in an R console

``` r

library('devtools')
install_github('graemeleehickey/joineRML')
```

# Compatibility with [`broom`](https://github.com/tidymodels/broom/)

Tidiers methods for objects of class `mjoint` (i.e. models fit with
`joineRML`) are included in the
[`broom`](https://github.com/tidymodels/broom/) package; this provides
methods that allow extracting model estimates, predictions, and
comparing models in a straightforward way.

See `vignette(topic = "joineRML-broom", package = "joineRML")` for
further details and examples.

# References

1.  Cox DR. Regression models and life-tables. *J R Stat Soc Ser B Stat
    Methodol.* 1972; **34(2)**: 187-220.

2.  Henderson R, Diggle PJ, Dobson A. Joint modelling of longitudinal
    measurements and event time data. *Biostatistics.* 2000; **1(4)**:
    465-480.

3.  Hickey GL, Philipson P, Jorgensen A, Kolamunnage-Dona R. Joint
    modelling of time-to-event and multivariate longitudinal outcomes:
    recent developments and issues. *BMC Med Res Methodol.* 2016;
    **16(1)**: 117.

4.  Laird NM, Ware JH. Random-effects models for longitudinal data.
    *Biometrics.* 1982; **38(4)**: 963-974.

5.  Lin H, McCulloch CE, Mayne ST. Maximum likelihood estimation in the
    joint analysis of time-to-event and multiple longitudinal variables.
    *Stat Med.* 2002; **21**: 2369-2382.

6.  Wulfsohn MS, Tsiatis AA. A joint model for survival and longitudinal
    data measured with error. *Biometrics.* 1997; **53(1)**: 330-339.

# Package index

## Model fitting

- [`mjoint()`](https://graemeleehickey.github.io/joineRML/reference/mjoint.md)
  : Fit a joint model to time-to-event data and multivariate
  longitudinal data

- [`mjoint.object`](https://graemeleehickey.github.io/joineRML/reference/mjoint.object.md)
  :

  Fitted `mjoint` object

- [`joineRML-package`](https://graemeleehickey.github.io/joineRML/reference/joineRML-package.md)
  : joineRML: Joint Modelling of Multivariate Longitudinal Data and
  Time-to-Event Outcomes

- [`joineRML`](https://graemeleehickey.github.io/joineRML/reference/joineRML.md)
  : joineRML

## Model summaries and inference

- [`summary(`*`<mjoint>`*`)`](https://graemeleehickey.github.io/joineRML/reference/summary.mjoint.md)
  :

  Summary of an `mjoint` object

- [`fixef(`*`<mjoint>`*`)`](https://graemeleehickey.github.io/joineRML/reference/fixef.mjoint.md)
  :

  Extract fixed effects estimates from an `mjoint` object

- [`ranef(`*`<mjoint>`*`)`](https://graemeleehickey.github.io/joineRML/reference/ranef.mjoint.md)
  :

  Extract random effects estimates from an `mjoint` object

- [`vcov(`*`<mjoint>`*`)`](https://graemeleehickey.github.io/joineRML/reference/vcov.mjoint.md)
  :

  Extract an approximate variance-covariance matrix of estimated
  parameters from an `mjoint` object

- [`confint(`*`<mjoint>`*`)`](https://graemeleehickey.github.io/joineRML/reference/confint.mjoint.md)
  :

  Confidence intervals for model parameters of an `mjoint` object

- [`getVarCov(`*`<mjoint>`*`)`](https://graemeleehickey.github.io/joineRML/reference/getVarCov.mjoint.md)
  :

  Extract variance-covariance matrix of random effects from an `mjoint`
  object

- [`sigma(`*`<mjoint>`*`)`](https://graemeleehickey.github.io/joineRML/reference/sigma.mjoint.md)
  :

  Extract residual standard deviation(s) from an `mjoint` object

- [`logLik(`*`<mjoint>`*`)`](https://graemeleehickey.github.io/joineRML/reference/logLik.mjoint.md)
  :

  Extract log-likelihood from an `mjoint` object

- [`formula(`*`<mjoint>`*`)`](https://graemeleehickey.github.io/joineRML/reference/formula.mjoint.md)
  :

  Extract model formulae from an `mjoint` object

## Prediction and dynamic prediction

- [`fitted(`*`<mjoint>`*`)`](https://graemeleehickey.github.io/joineRML/reference/fitted.mjoint.md)
  :

  Extract `mjoint` fitted values

- [`residuals(`*`<mjoint>`*`)`](https://graemeleehickey.github.io/joineRML/reference/residuals.mjoint.md)
  :

  Extract `mjoint` residuals

- [`dynSurv()`](https://graemeleehickey.github.io/joineRML/reference/dynSurv.md)
  : Dynamic predictions for the time-to-event data sub-model

- [`dynLong()`](https://graemeleehickey.github.io/joineRML/reference/dynLong.md)
  : Dynamic predictions for the longitudinal data sub-model

- [`baseHaz()`](https://graemeleehickey.github.io/joineRML/reference/baseHaz.md)
  :

  The baseline hazard estimate of an `mjoint` object

## Plotting and diagnostics

- [`plot(`*`<mjoint>`*`)`](https://graemeleehickey.github.io/joineRML/reference/plot.mjoint.md)
  :

  Plot diagnostics from an `mjoint` object

- [`plot(`*`<ranef.mjoint>`*`)`](https://graemeleehickey.github.io/joineRML/reference/plot.ranef.mjoint.md)
  :

  Plot a `ranef.mjoint` object

- [`plot(`*`<dynSurv>`*`)`](https://graemeleehickey.github.io/joineRML/reference/plot.dynSurv.md)
  :

  Plot a `dynSurv` object

- [`plot(`*`<dynLong>`*`)`](https://graemeleehickey.github.io/joineRML/reference/plot.dynLong.md)
  :

  Plot a `dynLong` object

- [`plotConvergence()`](https://graemeleehickey.github.io/joineRML/reference/plotConvergence.md)
  :

  Plot convergence time series for parameter vectors from an `mjoint`
  object

## Simulation, resampling, and uncertainty

- [`simData()`](https://graemeleehickey.github.io/joineRML/reference/simData.md)
  : Simulate data from a joint model

- [`sampleData()`](https://graemeleehickey.github.io/joineRML/reference/sampleData.md)
  :

  Sample from an `mjoint` object

- [`bootSE()`](https://graemeleehickey.github.io/joineRML/reference/bootSE.md)
  :

  Standard errors via bootstrap for an `mjoint` object

## Tidiers

- [`tidy(`*`<mjoint>`*`)`](https://graemeleehickey.github.io/joineRML/reference/mjoint_tidiers.md)
  [`augment(`*`<mjoint>`*`)`](https://graemeleehickey.github.io/joineRML/reference/mjoint_tidiers.md)
  [`glance(`*`<mjoint>`*`)`](https://graemeleehickey.github.io/joineRML/reference/mjoint_tidiers.md)
  : Tidying methods for joint models for time-to-event data and
  multivariate longitudinal data
- [`reexports`](https://graemeleehickey.github.io/joineRML/reference/reexports.md)
  [`augment`](https://graemeleehickey.github.io/joineRML/reference/reexports.md)
  [`glance`](https://graemeleehickey.github.io/joineRML/reference/reexports.md)
  [`tidy`](https://graemeleehickey.github.io/joineRML/reference/reexports.md)
  : Objects exported from other packages

## Data

- [`heart.valve`](https://graemeleehickey.github.io/joineRML/reference/heart.valve.md)
  : Aortic valve replacement surgery data
- [`epileptic.qol`](https://graemeleehickey.github.io/joineRML/reference/epileptic.qol.md)
  : Quality of life data following epilepsy drug treatment
- [`pbc2`](https://graemeleehickey.github.io/joineRML/reference/pbc2.md)
  : Mayo Clinic primary biliary cirrhosis data
- [`renal`](https://graemeleehickey.github.io/joineRML/reference/renal.md)
  : Renal transplantation data

# Articles

### All vignettes

- [joineRML and the broom
  package](https://graemeleehickey.github.io/joineRML/articles/joineRML-tidy.md):
- [joineRML](https://graemeleehickey.github.io/joineRML/articles/joineRML.md):
